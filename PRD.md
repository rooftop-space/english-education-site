# 영어교육사이트 재설계 PRD (핵심 반영본)

## 핵심 원칙 (변경)
이 프로젝트의 핵심은 **오프라인 영어사전/예문 데이터 구축 및 서비스 내 통합**이다.  
UI/기능은 이를 학습 경험으로 전달하는 레이어이며, 우선순위는 데이터 파이프라인 > 학습 로직 > 화면 개선 순으로 둔다.

---

## 1) 목표
- 로컬(오프라인) 사전 + 예문 DB를 구축해 안정적이고 빠른 학습 검색/추천 기반 확보
- 단어/뜻/품사/예문/난이도를 일관된 스키마로 관리
- 학습(신규) + 복습(SRS) + 추천(레벨 기반)을 데이터 중심으로 동작

## 2) 범위 재정의
### MVP 필수 범위
1. 오프라인 사전 데이터 적재 파이프라인
2. 오프라인 예문 데이터 적재 파이프라인
3. 통합 스키마(단어/의미/예문/난이도/출처/라이선스)
4. 사전/예문 검색 API(또는 로컬 조회 모듈)
5. 학습 세션/복습/SRS에서 사전+예문 실사용
6. 출처/라이선스 추적 필드 의무화

### UI 기능(온보딩/진도보드/구독)은 지원 범위
- 데이터 기반 학습 루프가 동작하는 것을 전제로 개선

---

## 3) 데이터 소스 정책 (오프라인)
- 우선 소스: **Open English WordNet + Tatoeba**
- 후보 소스: Wiktionary dump (라이선스 영향 검토 후)
- 원칙:
  - 배포 전 라이선스 재검증
  - source/license/source_ref 필드 필수
  - 생성 예문은 synthetic 플래그로 분리

---

## 4) 데이터 모델 (핵심)
- `words` (lemma, pos, level, freq, source)
- `senses` (definition, domain, license)
- `examples` (sentence, level, source, license)
- `sense_examples` (의미-예문 매핑)
- `word_forms` (활용형)

추가 원칙:
- 검색 최적화 인덱스(FTS/GIN 등) 필수
- 난이도는 CEFR-like 내부등급으로 통일

---

## 5) 구현 우선순위 (실행 순서)
1. 데이터 파서/정규화/적재 스크립트
2. 품질검증(중복/누락/라이선스 필드)
3. 조회 계층(검색/추천 기초)
4. 학습/복습 UI 연결
5. 운영 대시보드/관리자 고도화

---

## 6) 성공지표 (데이터 중심)
- 검색 성공률(결과 클릭 포함)
- 예문 노출 후 학습 완료율
- 복습 정답률 개선(주차별)
- 난이도 적합도(이탈/오답 기반)
- 데이터 품질 지표(누락률/중복률/라이선스 누락 0)

---

## 7) 즉시 액션 아이템
- [ ] `data/` 구조 생성(raw/processed)
- [ ] WordNet/OEWN ingest 스크립트 작성
- [ ] Tatoeba ingest 스크립트 작성
- [ ] 통합 스키마(sqlite 기준) 적용
- [ ] 앱 조회 로직을 샘플 DB에 연결
- [ ] 라이선스 고지 페이지 초안 추가

> 결론: 이 PRD에서 **최우선 deliverable은 오프라인 사전 데이터 인프라**이며, 나머지 기능은 이를 학습 UX로 연결하는 단계로 간주한다.
